# Backend only. Keep this file as a template; put real values in .env.
# SQLite remains the default and opens backend/inspections.db.
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/metrology
# METROLOGY_DATA_DIR=/absolute/path/to/persistent/data
SARVAM_API_KEY=
API_HOST=0.0.0.0
PORT=8000
TESSERACT_LANG=eng
